# e2e secrets

These are not "secrets" in the traditional sense.

That is, they are for the test stack only. It should never be exposed to the world. Therefore, these "secrets" are so the E2E `its-log` and data generation/test code can execute authentically. 