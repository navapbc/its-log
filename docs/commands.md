# commands

`its-log` provides multiple subcommands. This both provides the deployment surface as well as commands useful offline, in testing, and in CI/CD contexts.

## serve

```
its-log serve
```

The configuration file is used to set the host and port that the API server runs on. 